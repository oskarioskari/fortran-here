Compile program by using following
commands in command line:

> gfortran -c constants.f90 definitions.f90 main.f90
> gfortran -o simulation.exe constants.o definitions.o main.o

 - - -

To run program, check README.txt
in /run for instructions.
Program can be run after that by
executing simulation.exe:

> ./simulation.exe
